# Notice

Sweep packages and adapts the existing local `macos-storage-cleanup` skill for
the `$sweep:cleanup` plugin namespace. Its audit script and classification
rules are additionally hardened against ambiguous process checks, dynamic
set-wide simulator deletion, and untrusted path output.
